{
  "scripts": {
    "dev": "nodemon index.js",
    "start": "pm2 start index.js --name 'Voice Channel Logger'",
    "stop": "pm2 stop 'Voice Channel Logger'",
    "restart": "pm2 restart 'Voice Channel Logger'"
  },
  "dependencies": {
    "discord.js": "^14.7.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
